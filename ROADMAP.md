# Roadmap

The roadmap for craft, the design and copy standards, is
[`packages/craft/ROADMAP.md`](packages/craft/ROADMAP.md). It is the only one.

This file used to hold the "design-check" roadmap (`design-check.py`, a
`skill-design` skill, a hook in the house-style repo). None of those exist any
more: craft replaced all of them in phases A to F. The old text is in git
history if it is ever needed.
