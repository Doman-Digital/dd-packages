---
"@domandigital/create-site": patch
---

New sites install `@domandigital/craft@^0.14.0`, the release with `specificity`, the `generic-hero-claim` and `unproven-claim` tells and `craft copy compare`.
